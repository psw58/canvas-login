// Sass configuration
{
    // initialize for use with windows shell
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Gulp",
            "command": "gulp",
            "args": ["default"],
            "type": "shell",
            "group": {
              "kind": "build",
              "isDefault": true
            }
          }
    ],
}